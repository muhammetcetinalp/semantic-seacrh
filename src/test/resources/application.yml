search:
  opensearch:
    host: localhost
    port: 9200
    scheme: http
  embedding:
    mock-enabled: true
    dimensions: 384
  defaults:
    limit: 20
    max-limit: 100
    default-search-type: HYBRID
    default-index-name: entities
